// STORE: demandes.js
// Gère: demandes devis CRUD, createDemande, respondDevis

import { defineStore } from 'pinia'

export const useDemandesStore = defineStore('demandes', {
  state: () => ({
    // TODO: demandes array, loading, currentDemande
  }),
  getters: {
    // TODO: getters
  },
  actions: {
    // TODO: actions (createDemande, fetchMyDemandes, fetchAllDemandes, respondreDevis)
  },
})
