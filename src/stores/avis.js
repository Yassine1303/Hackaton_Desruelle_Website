// STORE: avis.js
// Gère: avis clients (notes 1-5 + commentaires)

import { defineStore } from 'pinia'

export const useAvisStore = defineStore('avis', {
  state: () => ({
    // TODO: avis array, loading
  }),
  getters: {
    // TODO: getters
  },
  actions: {
    // TODO: actions (createAvis, fetchAvisForPrestation, fetchMyAvis)
  },
})
