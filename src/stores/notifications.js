// STORE: notifications.js
// Gère: toast notifications + centre notifications

import { defineStore } from 'pinia'

export const useNotificationsStore = defineStore('notifications', {
  state: () => ({
    // TODO: notifications array, unreadCount
  }),
  getters: {
    // TODO: getters
  },
  actions: {
    // TODO: actions (addNotification, markAsRead, clearAll, fetchNotifications)
  },
})
