// STORE: disponibilites.js
// Gère: disponibilites techs, add/remove, getTechsLibres

import { defineStore } from 'pinia'

export const useDisponibilitesStore = defineStore('disponibilites', {
  state: () => ({
    // TODO: disponibilites array, loading
  }),
  getters: {
    // TODO: getters
  },
  actions: {
    // TODO: actions (fetchMyDisponibilites, addDisponibilite, removeDisponibilite, getTechsLibres)
  },
})
