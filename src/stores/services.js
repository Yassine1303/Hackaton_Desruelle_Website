// STORE: services.js
// Gère: services catalogue, fetchServices, getServiceById

import { defineStore } from 'pinia'

export const useServicesStore = defineStore('services', {
  state: () => ({
    // TODO: services array, loading
  }),
  getters: {
    // TODO: getters (servicesByCategorie)
  },
  actions: {
    // TODO: actions (fetchServices, getServiceById)
  },
})
