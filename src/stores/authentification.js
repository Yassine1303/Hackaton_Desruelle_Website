// STORE: authentification.js
// Gère: login, logout, register, user state, isAuthenticated, userRole

import { defineStore } from 'pinia'

export const useAuthStore = defineStore('authentification', {
  state: () => ({
    // TODO: state properties
  }),
  getters: {
    // TODO: getters
  },
  actions: {
    // TODO: actions (login, logout, register, updateUser)
  },
})
