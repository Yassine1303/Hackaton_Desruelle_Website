// SERVICE: avis.service.js
// Endpoints: CRUD avis (notes + commentaires)

import axios from 'axios'

export const avisService = {
  createAvis(prestation_id, data) {
    // TODO: POST /avis
  },
  getAvisForPrestation(id) {
    // TODO: GET /avis?prestation_id=id
  },
}
