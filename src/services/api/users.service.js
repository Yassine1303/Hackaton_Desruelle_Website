// SERVICE: users.service.js
// Endpoints: getMe, updateMe, CRUD users (respo only)

import axios from 'axios'

export const usersService = {
  getMe() {
    // TODO: GET /users/me
  },
  updateMe(data) {
    // TODO: PUT /users/me
  },
  getAllUsers() {
    // TODO: GET /users (respo)
  },
  createUser(data) {
    // TODO: POST /users (respo)
  },
  updateUser(id, data) {
    // TODO: PUT /users/:id (respo)
  },
  deleteUser(id) {
    // TODO: DELETE /users/:id (respo)
  },
}
